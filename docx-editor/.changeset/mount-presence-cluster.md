---
'@casualoffice/docs': minor
---

CasualEditor now mounts the PresenceCluster (peer avatars + Live/Connecting/Offline badge) in the editor title bar whenever collab is active, fed by the live peers and connection status. Standalone (single-user) docs are unaffected. A new optional `onShare` prop wires the cluster's Share button.
